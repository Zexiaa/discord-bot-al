export const db_directory = "./al_database.db"

// Tables
export const db_reminderTable = 'reminder_message';
export const db_eventsTable = 'live_events';

// Events
export const event_reminderTrigger = "remindertrigger";